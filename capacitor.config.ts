import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.chronodex.app',
  appName: 'chronodex-app',
  webDir: 'dist/chronodex-app/browser'
};

export default config;
